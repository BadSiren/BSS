using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using BSS.Unit;

namespace BSS.UI {
	public class UIControl : MonoBehaviour
	{
		/*
		public void unitInfoShow() {
			var unitInfo=Board.boardList.Find (x => x is UnitInfo) as UnitInfo;
			unitInfo.Show (gameObject);
		}
		public void unitInfoClear() {
			var unitInfo=Board.boardList.Find (x => x is UnitInfo) as UnitInfo;
			unitInfo.clearSelectUnit ();
		}
		public void activeBoardShow() {
			var activeBoard=Board.boardList.Find (x => x is ActiveBoard) as ActiveBoard;
			BaseUnit unit=GetComponent<BaseUnit> ();
			var actList=new List<Activable>(GetComponentsInChildren<Activable> ());
			activeBoard.setActivable (actList, unit);
		}
		*/
	}
}

